# Copy to `.env` in the project root and set values.
# Used by python-dotenv when the pipeline runs (see kgs_rnd_ontoverse.utils.kg_db).

NEO4J_PASSWORD=your_neo4j_password
NEO4J_DB=neo4j
ZOTERO_SQLITE_PATH=/Users/yourname/Zotero/zotero.sqlite
